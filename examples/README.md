# Fictional Example

`fictional-change-evidence-packet.json` is a sanitized example for an imaginary inventory service. All paths, identifiers, hashes, event names, and text are fictional. It demonstrates the packet shape only; it is not proof of a real change.
